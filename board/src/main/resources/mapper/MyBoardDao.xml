<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.demo.dao.IBoardDao">
<select id="getList" resultType="com.example.demo.dto.BoardDto">
	select * from board
</select>
<select id="getList2" resultType="com.example.demo.dto.BoardDto">
	select * from board
	<where>
		<if test="keyword != null and keyword != ''"> <!-- 키워드가 넘어왔는지 확인 -->
			<choose>
				<when test='type == "t"'>
					title LIKE '%'|| #{keyword} || '%'
				</when>
				<when test='type == "c"'>
					content LIKE '%' || #{keyword} || '%'
				</when>
				<when test='type == "w"'>
					writer LIKE '%' || #{keyword} || '%'
				</when>
				<when test='type =="tc"'>
					title LIKE '%' || #{keyword} || '%' or
					content LIKE '%' || #{keyword} || '%'
				</when>
				
			</choose>
		</if>
	</where>
</select>

<select id="getList3" resultType="com.example.demo.dto.BoardDto">
	select * from board
	<where>
		<if test="keyword != null and keyword != ''">
			<trim prefix="(" suffix=")" prefixOverrides="OR">
				<if test='type != null and type.contains("t")'>
					or title like '%' || #{keyword} || '%'
				</if>
				<if test='type != null and type.contains("c")'>
					or content like '%' || #{keyword} || '%'
				</if>
				<if test='type != null and type.contains("w")'>
					or writer like '%' || #{keyword} || '%'
				</if>
			</trim>
		</if>
	</where>
</select>

</mapper>